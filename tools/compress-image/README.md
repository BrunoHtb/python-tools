# Compress Image

Batch optimizer that automatically reduces image file size to a defined limit.

## What it does

- Recursively scans a directory
- Detects images larger than the target size
- Dynamically adjusts compression quality
- Overwrites the original file once the size threshold is reached

## Configuration

Create a `.env` file:

